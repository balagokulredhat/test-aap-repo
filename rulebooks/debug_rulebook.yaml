---
- name: Run a webhook listener service
  hosts: all
  sources:
    - ansible.eda.webhook:
        port: 5005
  rules:
    - name: Webhook event
      condition: event.payload is defined
      action:
        debug:
          msg: "{{ event.payload }}"
